import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { UIProvider } from '../UIProvider';

import { Layout, Box, Button, Flex } from "../";

import { FaBeer } from "../icons";

<Meta title="components/Layout" component={Layout}
  decorators={[
    Story => (
      <UIProvider>
        <Story />
      </UIProvider>
    ),
  ]}
  argTypes={{
    variant: {
      control: {
        type: "select",
        options: ["centered", "fluidStack"],
      },
    },
    themeKey: {
      defaultValue: "layout",
    },
  }}
/>

# Layout

## Centered

<Canvas>
  <Story name="Centered" style={{ height: "100%" }}>
<Layout look="centered" width={1} height="100%" p={20} border="1px solid green">
  <Button>Totally Centered</Button>
</Layout>
  </Story>
</Canvas>

## Stack

### With gap and padding

<Story id="components-layout-stack--with-gap-and-padding" />

### With no gap or padding

<Story id="components-layout-stack--default-stack" />

## Responsive Stack

### Mobile

<Canvas style={{ width: "500px" }}>
  <Story name="Responsive Stack (mobile)" inline={false}>
<Layout look="responsiveStack" p={1} gap={1} border="1px solid green">
  <Button size="sm">First</Button>
  <Button size="sm">Second</Button>
  <Button size="sm">Third</Button>
</Layout>
  </Story>
</Canvas>

### Tablet / Desktop

<Canvas>
  <Story name="Responsive Stack (tablet/desktop)">
<Layout look="responsiveStack" p={1} gap={1} border="1px solid green">
  <Button size="sm">First</Button>
  <Button size="sm">Second</Button>
  <Button size="sm">Third</Button>
</Layout>
  </Story>
</Canvas>

### Responsive Stack with space before

<Canvas>
  <Story name="Responsive Stack/space before">
<Layout look="responsiveStack" p={2} gap={2} space="before" border="1px solid green">
  <Button size="sm">First</Button>
  <Button size="sm">Second</Button>
</Layout>
  </Story>
</Canvas>

### Responsive Stack with space between

<Canvas>
  <Story name="Responsive Stack/space between">
<Layout look="responsiveStack" p={2} gap={2} space="between" border="1px solid green">
  <Button size="sm">First</Button>
  <Button size="sm">Second</Button>
</Layout>
  </Story>
</Canvas>

### Responsive Stack with space around

<Canvas>
  <Story name="Responsive Stack with space around">
<Layout look="responsiveStack" p={2} gap={2} space="around" border="1px solid green">
  <Button size="sm">First</Button>
  <Button size="sm">Second</Button>
</Layout>
  </Story>
</Canvas>
